{% assign icons =  "images/demo/icon-0.png,images/demo/icon-1.png,images/demo/icon-2.png,images/demo/icon-3.png,images/demo/icon-4.png,images/demo/icon-5.png,images/demo/icon-6.png,images/demo/icon-7.png,images/demo/icon-8.png,images/demo/icon-9.png" | split: ',' %}
{% assign card_pages = site.pages['/'].children_in_nav_menu  | limit: 24 %}

<!-- Hero Section -->
<div class="bg-gradient-to-r from-blue-50 to-blue-100 py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <h1 class="text-4xl font-bold text-gray-900 sm:text-5xl md:text-6xl">
        {{ page.settings.slogan |  default: 'Quick to Create, Easy to Organize' }}
      </h1>
      <p class="mt-3 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
        {{ page.settings.description |  default: 'The easiest way to create online user manuals for your next product.' }}
      </p>

      <!-- Search Box -->
      <div class="mt-10 max-w-xl mx-auto">
        <div x-data="{ query: '' }" class="relative">
          <input type="text" x-model="query" x-on:click="searchOpen = !searchOpen" placeholder="Have a question? Ask or enter a search term." 
                class="w-full px-12 py-3 border border-gray-300 rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500">
          <div class="absolute left-4 top-3.5">
            <svg class="h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
            </svg>
          </div>
        </div>

        <!-- Popular Searches -->
        <div class="mt-4 text-sm text-gray-500">
          <span>{{ 'generic.popular_search' | t }}:</span>
          {% for tag in page.settings.tags %}
            <a href="{{ tag.path }}" class="text-blue-600 hover:text-blue-800 mx-1">{{ tag.name }}</a>
            {%- unless forloop.last -%}, {%- endunless -%} 
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    {% for card_page in card_pages %}
      {%  assign sub_pages = card_page.children_in_nav_menu  %}
      {%  assign sub_page_count = sub_pages| size %}
      <!-- Corporate Section -->
      <div class="bg-white rounded-lg shadow-lg p-8 border border-blue-50">
        <div class="flex flex-col items-start mb-6">
          <div class="mb-4">
            {% if card_page.settings.icon %}
              <img src="{{ card_page.settings.icon }}" alt="{{ card_page.link_text }}" class="w-14 h-14" />
            {%  else %}
              {% assign icon_index = forloop.index | modulo: 10 %}
              <img src="{{ icons[icon_index] |  asset_url }}" alt="{{ card_page.link_text }}" class="w-14 h-14" />              
            {% endif %}
          </div>
          <div class="text-start">
            <h3 class="text-xl font-semibold mb-1"><a href="{{ card_page.path | append: '?list=true' }}" class="hover:text-blue-600">{{ card_page.link_text }}</a></h3>
            <p class="text-gray-400">{{ sub_page_count }} {{ 'generic.articles' | t }}</p>
          </div>
        </div>

        <ul class="space-y-3 mb-6 overflow-x-hidden">
          {% for sub_page in sub_pages | limit: 5 %}
            {% assign ssub_pages = sub_page.children_in_nav_menu %}
            {% assign ssub_page_count = ssub_pages | size %}
            {% if ssub_page_count > 0 %}
              <li><a href="{{ sub_page.path }}" class="text-gray-600 hover:text-blue-600 flex space-x-2 items-center">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6 w-5 h-5 mr-1">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12.75V12A2.25 2.25 0 0 1 4.5 9.75h15A2.25 2.25 0 0 1 21.75 12v.75m-8.69-6.44-2.12-2.12a1.5 1.5 0 0 0-1.061-.44H4.5A2.25 2.25 0 0 0 2.25 6v12a2.25 2.25 0 0 0 2.25 2.25h15A2.25 2.25 0 0 0 21.75 18V9a2.25 2.25 0 0 0-2.25-2.25h-5.379a1.5 1.5 0 0 1-1.06-.44Z" />
                </svg>
                <span>{{ sub_page.link_text }}  [{{ ssub_page_count }} {{ 'generic.articles' | t }}] </span>
                </a></li>
            {%  else %}
              <li><a href="{{ sub_page.path }}" class="text-gray-600 hover:text-blue-600 flex space-x-2 items-center">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6 w-5 h-5 mr-1">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z" />
                </svg>
                <span>{{ sub_page.link_text }}</span>
                </a></li>
            {% endif %}
          {% endfor %}
        </ul>

        <div>
          <a href="{{ card_page.path | append: '?list=true' }}" class="inline-flex items-center text-blue-600 hover:text-blue-800">
            {{ 'generic.read_more' | t }}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
      </div>
    {% endfor %}

  </div>

  <!-- FAQ Section -->
  <div class="mt-20">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-bold mb-4">Frequently Asked Questions</h2>
      <p class="text-gray-600 mb-12">I am text block. Click edit button to change this text. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Ut elit tellus, luctus nec ullamcorper mattis, pulvinar dapibus leo.</p>
    </div>

    <div class="max-w-3xl mx-auto">
      <div class="space-y-4">
        <div class="bg-blue-50 rounded-lg">
          <button class="w-full text-left px-6 py-4 font-medium text-gray-900 hover:text-blue-600">
            Where can I find license key
          </button>
          <div class="px-6 py-4 bg-white rounded-b-lg hidden">
            <p class="text-gray-600 mb-4">Lorem ipsum dolor sit amet, consectetur adipisicing elit...</p>
            <div class="flex space-x-4">
              <a href="#" class="text-gray-400 hover:text-blue-600"><i class="fab fa-twitter"></i></a>
              <a href="#" class="text-gray-400 hover:text-blue-600"><i class="fab fa-facebook-f"></i></a>
              <a href="#" class="text-gray-400 hover:text-blue-600"><i class="fab fa-pinterest-p"></i></a>
              <a href="#" class="text-gray-400 hover:text-blue-600"><i class="far fa-envelope"></i></a>
            </div>
          </div>
        </div>

        <!-- Additional FAQ items follow same pattern -->
      </div>
    </div>
  </div>

</div>

{% render 'cta' %}

{% schema %}
  {
    "name": "Docs Center",
    "description": "Ideal for feature-rich product user manuals",
    "thumb_url": "images/theme/index.webp",
    "preview_image_urls": [
      "images/theme/index.webp"
    ],
    "sub_page_templates": ["page"],
    "settings": [
        {
          "id": "slogan",
          "type": "text",
          "label": "站点首页口号",
          "default": "Frequently Asked Questions"
        },
        {
          "id": "description",
          "type": "text",
          "label": "t:settings_schema.generic.settings.description.label",
          "default": "👋 What Can We Help?"
        },
        {
          "id": "tags",
          "type": "tag_picker",
          "multiple": true,
          "label": "t:settings_schema.page.settings.tags.label"
        }
      ]
  }
{% endschema %}